apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: furry-train-web-app
spec:
  routes:
    - conditions:
      - prefix: /
      services:
      - name: furry-train-web-app
        port: 80
